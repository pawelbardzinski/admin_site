describe('Foo', function() {
  it("does something", function() {
    expect(1 + 1).toBe(2);
  });
});
describe('Foo', function() {
  it("does something", function() {
    expect(2 + 1).toBe(3);
  });
});
